import { useState } from "react";
import { useQuery } from "react-query";
import { LinearProgress } from "./components";

const App = () => {
  return (
    <div>
      <LinearProgress />
    </div>
  );
};

export default App;
